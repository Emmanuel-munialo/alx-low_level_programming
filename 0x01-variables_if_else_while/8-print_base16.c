#include <stdio.h>
/**
 * main -  prints all the numbers of base 16 in lowercase
 * Return: 0
 */
int main(void)
{
int d;
char l;
for (d = 0; i <= 9; d++)
putchar(d);
for (l = 'a'; l <= 'f'; l++)
putchar(l);
putchar('\n');
return (0);
}
